import type { Product } from "../../types"

interface Props {
  product:Product
}

const ProductCard = ({product} : Props) => {
   return (
    <div>
      
    </div>
  )
}

export default ProductCard
